# 18F

## Manual review status
- Category: `Open Data`
- Provider slug: `18f`
- Official docs/pages used:
  - `http://18f.github.io/API-All-the-X/` (the indexed official docs URL; during review it resolved to the GitHub Pages host `https://18f.github.io/API-All-the-X/`)
  - `https://github.com/18F/API-All-the-X` (official repository under the `18F` GitHub organization)
- Manual review outcome: the historical site is no longer published and the surviving official repository does not expose a current public API route inventory
- Route count confirmed: `0`

## Blocker summary
- The indexed official docs URL now returns GitHub Pages `404` with the message `There isn't a GitHub Pages site here.`
- The official GitHub repository is still reachable, but it describes itself as `Resources and Materials for the /Developer Program` and as the source for the retired site content.
- The reviewed repository page exposes repository metadata, folders, and contribution instructions, but it does not provide a live API base URL, endpoint table, auth contract, request schema, pagination model, or error reference for a current public API.
- Because the official pages reviewed in this pass do not expose an actual route-by-route API contract, fireROUTE cannot safely normalize any public endpoints from official material alone.

## Evidence from manual inspection
- Visiting `http://18f.github.io/API-All-the-X/` resolved to `https://18f.github.io/API-All-the-X/` and displayed the GitHub Pages error page titled `Site not found · GitHub Pages`.
- The visible page text states: `There isn't a GitHub Pages site here.`
- Visiting `https://github.com/18F/API-All-the-X` loaded the official repository page titled `GitHub - 18F/API-All-the-X: Resources and Materials for the /Developer Program · GitHub`.
- The repository README shown on that page describes the project as `/Developer Program` material and says `the contents of this project now be found in this folder`, which is consistent with a repository/site source rather than a maintained public API reference.

## What was still confirmable from official pages
- The project is associated with the official `18F` GitHub organization.
- The surviving official artifact is a public GitHub repository rather than an accessible live API documentation site.
- No current public API base URL, auth scheme, endpoint list, or parameter catalog was visible on the reviewed official pages.

## fireROUTE note
- Treat `18F` as manually documented with an explicit blocker and `0` confirmed public routes in the current environment.
- If this provider is revisited later, start by checking whether 18F has republished the GitHub Pages site or moved the `/Developer Program` route inventory to a new official host.

## Sources inspected
- `http://18f.github.io/API-All-the-X/`
- `https://18f.github.io/API-All-the-X/`
- `https://github.com/18F/API-All-the-X`
