# Image Upload

## Overview
- Provider: Image Upload API
- Category: Photography
- Official docs URL from index: `https://apilayer.com/marketplace/image_upload-api`
- Documentation status: explicit blocker after manual review
- Confirmed public route count: `0`

## Blocker summary
The APILayer marketplace listing for Image Upload API is still live, but the publicly visible page reviewed in this run did not expose a concrete endpoint list.

- `https://apilayer.com/marketplace/image_upload-api` redirects to `https://marketplace.apilayer.com/image_upload-api`
- the public marketplace page identifies the product as `Image Upload API` and describes it as `an image manipulation and optimization REST API`
- the reviewed public page exposes pricing and tabs for `API info` / `Documentation`, but the visible content did not reveal concrete method + path combinations, a base URL, auth header names, or parameter schemas

## What was and was not confirmable
- Marketplace/product URL: `https://marketplace.apilayer.com/image_upload-api`
- Product description: image manipulation and optimization REST API
- Provider family: APILayer marketplace product
- Concrete API base URL: not confirmable from the inspected public page
- Endpoint paths and HTTP methods: not confirmable
- Auth details: the old index labeled the provider as `apiKey`, but the visible public marketplace page did not expose the concrete request format
- Rate limits, pagination, errors, and response format: not confirmable from the visible public page

## Important usage note
fireROUTE should keep this provider blocked until APILayer exposes a readable public route reference for the Image Upload product rather than only the marketplace shell.

## Sources inspected
- `https://apilayer.com/marketplace/image_upload-api`
- `https://marketplace.apilayer.com/image_upload-api`