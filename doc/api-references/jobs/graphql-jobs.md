# GraphQL Jobs

## Provider metadata
- Category: `Jobs`
- Provider slug: `graphql-jobs`
- Official pages checked:
  - `https://graphql.jobs/docs/api/`
  - `https://graphql.jobs/`
- Manually confirmed route count: `0`

## Manual review result
Both the official docs URL and the main site failed DNS resolution during manual browser review.

## Explicit blocker note
- The assigned official docs URL returned a name-resolution failure.
- The root official domain also returned a name-resolution failure.
- Because the official host itself was unreachable, I could not verify any live endpoint, base URL, auth model, or response format from the provider's own documentation.

## fireROUTE note
- Treat as provider-blocked/unreachable pending restoration of the official domain.
