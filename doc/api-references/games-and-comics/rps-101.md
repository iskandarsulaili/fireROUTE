# RPS 101

## Overview
- Provider: `RPS 101`
- Category: `Games & Comics`
- Official docs URL: `https://rps101.pythonanywhere.com/api`
- Official pages inspected manually in this execution:
  - `https://rps101.pythonanywhere.com/`
  - `https://rps101.pythonanywhere.com/api`
- Manual status: `manual_blocked`
- Confirmed route count: `0`

## What the official site showed
- Both the assigned root URL and the `/api` URL loaded with the title `Coming Soon: PythonAnywhere`.
- Both pages displayed PythonAnywhere placeholder hosting content headed by `Coming Soon!`.
- The visible text included PythonAnywhere's generic service description plus a `Developer info` section instructing the site owner to create a web app for the domain.
- No provider-authored route catalog, method reference, authentication guide, parameter documentation, rate-limit policy, pagination notes, error schema, or example responses were visible from the provider-controlled host.

## Confirmed integration details
- Stable official API base URL: not publicly confirmable from the current official host.
- Endpoint paths manually tested:
  - `/`
  - `/api`
- Methods confirmed in this execution:
  - anonymous browser `GET` requests to the listed public URLs
- Parameters: not publicly confirmable from the current official host.
- Authentication: not publicly confirmable from the current official host.
- Rate limits: not publicly confirmable from the current official host.
- Pagination: not publicly confirmable from the current official host.
- Errors and failure behavior confirmed manually:
  - `/` -> placeholder page titled `Coming Soon: PythonAnywhere`
  - `/api` -> placeholder page titled `Coming Soon: PythonAnywhere`
- Format notes: the only publicly visible output was placeholder HTML, not a trustworthy API response or documentation surface.
- Important usage notes:
  - the assigned host currently serves PythonAnywhere placeholder pages instead of a live first-party API contract
  - fireROUTE should not infer route coverage from stale examples or secondary references when the current provider-controlled host exposes no verifiable endpoint contract

## Why this remains blocked for fireROUTE
- fireROUTE needs a provider-controlled API/docs page with verifiable route details.
- Both reviewed official URLs returned only PythonAnywhere placeholder content.
- Without a live first-party contract, fireROUTE cannot safely confirm routes, methods, parameters, auth requirements, rate limits, pagination, errors, or response formats.

## fireROUTE integration note
- Keep this provider in `manual_blocked` state until the provider restores a working official API/docs host.
- Keep the confirmed route count at `0` until official endpoint details are publicly visible and verifiable.

## Sources inspected
- `https://rps101.pythonanywhere.com/`
- `https://rps101.pythonanywhere.com/api`
