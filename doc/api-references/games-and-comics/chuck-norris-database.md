# Chuck Norris Database

## Overview
- Provider: `Internet Chuck Norris Database (ICNDB)`
- Category: `Games & Comics`
- Official docs URL from index: `http://www.icndb.com/api/`
- Official alternative page reviewed in this execution: `https://api.icndb.com/`
- Manual status: `manual_blocked`
- Confirmed route count: `0`

## Manual official-site findings
- Browsing `http://www.icndb.com/api/` redirected to `https://icndb.com/`.
- Browsing `https://api.icndb.com/` also resolved to `https://icndb.com/`.
- The resolved page title on both paths was `ROYALTOGEL Situs Slot Gacor Terpercaya 2025 – Bonus Besar & Mudah Jackpot`.
- The visible body on the current live domain was unrelated gambling content beginning with `RTP ROYALTOGEL`, `LOGIN`, `DAFTAR`, and `SELAMAT DATANG DI SITUS ROYALTOGEL SLOT GACOR RESMI INDONESIA`.
- No trustworthy ICNDB API documentation, endpoint list, parameter reference, authentication guide, rate-limit note, pagination contract, error model, or sample API payloads were exposed on the currently reachable first-party surfaces.

## Confirmed fireROUTE integration facts
- Current live resolved host from the reviewed official URLs: `https://icndb.com/`
- Confirmed request method used in this review: anonymous browser `GET` to the listed official URLs
- Trustworthy ICNDB API base URL: not verifiable from the currently reachable official surfaces
- Confirmed endpoint paths: none exposed on the currently reachable official surfaces
- Confirmed HTTP methods beyond the browser page loads above: none exposed
- Confirmed parameters: none exposed
- Authentication: not exposed
- Rate limits: not exposed
- Pagination: not exposed
- Error schema: not exposed
- Response-format notes: no trustworthy ICNDB API responses or schemas were visible on the currently reachable official surfaces
- Important usage note: the historical official ICNDB documentation hosts are currently repointed to unrelated content, so fireROUTE cannot safely infer an API contract from them

## Explicit blocker
- The historical official ICNDB documentation URLs no longer expose recognizable ICNDB API material.
- Because the currently live official surfaces resolve to unrelated non-API content, fireROUTE cannot verify a canonical base URL, route inventory, parameters, auth model, pagination behavior, error contract, or official route count from official material.

## Sources inspected in this execution
- `http://www.icndb.com/api/`
- `https://api.icndb.com/`
