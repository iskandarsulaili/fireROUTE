# Escape

## Provider metadata
- Category: `Security`
- Provider slug: `escape`
- Docs used manually:
  - `https://github.com/polarspetroll/EscapeAPI`
  - `https://raw.githubusercontent.com/polarspetroll/EscapeAPI/master/README.md`
  - live host check against the base URL published in the README: `https://s.polarspetroll.repl.co/`
- Confirmed documented API base URL from the official README: `https://s.polarspetroll.repl.co`
- Primary response format: JSON
- Authentication model confirmed from the reviewed official README:
  - no authentication documented
- Manually confirmed routes in this pass: `1`

## Authentication
- The official README documents query-driven use only and does not mention API keys, bearer tokens, cookies, or login requirements.
- No authentication header or credential field appears in any official example.

## Common request/response conventions
- The official README describes Escape as `an API for escaping html, shell and path queries`.
- All official examples use a single `GET` endpoint with query parameters.
- The README documents three escaping modes:
  - `html` - HTML escaping for XSS-oriented use cases
  - `shell` - Bourne shell escaping for command-injection prevention
  - `path` - path escaping for directory-traversal prevention
- Every example response uses a small JSON envelope containing:
  - `ok`
  - `data`

## Manually confirmed endpoint inventory
1. `GET /api` - escape user-supplied input according to the selected `method`

Manual route count confirmed: **1**.

## Parameters and request-body notes
- Confirmed query parameters from the official README:
  - `method` - escaping strategy; documented values are `html`, `shell`, and `path`
  - `data` - raw input string to escape
- No request body is documented.
- No path parameters are documented.

## Pagination
- No pagination model is documented.
- The API shape shown in the README is request-per-string transformation, not collection retrieval.

## Rate limits
- No rate-limit, quota, or abuse-control documentation appears in the official README.

## Error and response notes
- The official README only publishes successful example responses.
- No formal error payload schema or HTTP-status table is documented in the repository README.
- The success examples show JSON like:
  - `{"ok":true,"data":"..."}`

## Response format notes
- Responses are documented as JSON.
- The returned `data` value is the escaped string corresponding to the selected method.
- The official examples demonstrate method-specific transformations for HTML entities, shell metacharacters, and path traversal segments.

## Important usage notes
- The official repository documents only one route surface, `GET /api`, with behavior fully controlled by the `method` query parameter.
- The documented published host remains `https://s.polarspetroll.repl.co`, but a direct live navigation check to that host failed with `net::ERR_NAME_NOT_RESOLVED` in this environment. The route contract above is therefore grounded in the official repository README rather than a currently reachable live deployment.
- Because the official material is README-only, this file does not invent undocumented error handling, rate limits, or secondary endpoints.

## Verification notes
This file was manually rebuilt from the official EscapeAPI repository and its raw README, replacing the earlier autogenerated placeholder.