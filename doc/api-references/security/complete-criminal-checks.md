# Complete Criminal Checks

## Provider metadata
- Category: `Security`
- Provider slug: `complete-criminal-checks`
- Docs used manually:
  - `https://completecriminalchecks.com/Developers`
  - `https://completecriminalchecks.com/members/docs.php`
- Confirmed API base URL from the reviewed official page: `https://completecriminalchecks.com/api`
- Confirmed production route prefix from the reviewed official examples: `https://completecriminalchecks.com/api/search.php`
- Primary response format: JSON
- Authentication model confirmed from the reviewed official page:
  - API key sent in the `X-API-Key` request header
- Manually confirmed routes in this pass: `1`

## Authentication
- The public developer landing page explicitly advertises `API key-based authentication with secure HTTPS connections`.
- Every official code example shown on the page sends the key in the `X-API-Key` header.
- The linked deeper documentation page at `https://completecriminalchecks.com/members/docs.php` redirected to the member login page, so no second public auth mechanism was exposed during anonymous review.

## Common request/response conventions
- The reviewed public page presents the API as a simple REST/JSON service.
- The public examples only expose one search route and show it as an HTTP `GET` request.
- The page markets the search as one unified API over four feeds:
  - `arrest`
  - `sex_offender`
  - `inmate`
  - `most_wanted`
- The example JSON response uses a top-level structure containing:
  - `success`
  - `credits_used`
  - `remaining_credits`
  - `results` keyed by feed name

## Manually confirmed endpoint inventory
1. `GET /api/search.php` - search the provider's criminal-record feeds by person name and requested feed set

Manual route count confirmed: **1**.

## Parameters and request-body notes
- Publicly shown query parameters:
  - `name` - search name string; all official examples use values like `John Doe`
  - `feeds` - comma-separated feed list; the public examples use `arrest,sex_offender,inmate,most_wanted`
- Publicly shown header parameters:
  - `X-API-Key` - required API key header shown in cURL, PHP, and JavaScript examples
- No request body is documented on the public page; all official examples use query parameters plus the API-key header.
- The public page did not expose a formal machine-readable parameter table for optional filters beyond `name` and `feeds`.

## Pagination
- No pagination parameters, cursor model, or paged-response envelope were documented on the public developer page.
- The public examples present a single-result JSON response structure without any paging metadata.

## Rate limits and billing notes
- No rate-limit window or requests-per-minute quota table was exposed on the public page.
- The official page does clearly document a credit model:
  - `1 credit = 1 search`
  - credits are sold in packages
  - the signup flow advertises `25 FREE Credits`
- Because the deeper docs are login-gated, this file does not invent undocumented throttling numbers.

## Error and response notes
- The public page shows a successful JSON response example only.
- The reviewed anonymous materials did not publish a global HTTP error-code table or a formal error schema.
- The JavaScript example handles failures with a generic `catch (error)` block, which confirms that request failures should be expected, but it does not define the provider's canonical error payload.

## Response format notes
- The public examples consistently describe the API as returning JSON.
- Successful example output includes feed-specific arrays nested under `results`.
- The page repeatedly emphasizes `JSON Responses` and `Simple REST API` rather than alternate formats such as XML or CSV.

## Important usage notes
- The public anonymous page confirms only one route surface, `GET /api/search.php`, plus the `name` and `feeds` query contract.
- The page frames the service as a credit-backed search API rather than an unlimited subscription API.
- The deeper `API Documentation` link is currently behind member login, so any undisclosed filters, response fields, or operational limits remain unverified from public official docs.
- The page also includes a legal disclaimer stating the data is sourced from public records and should not be used as the sole basis for legally protected decisions.

## Verification notes
This file was manually rebuilt from the live Complete Criminal Checks developer landing page and the linked official documentation/login destination, replacing the earlier autogenerated placeholder.