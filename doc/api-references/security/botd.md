# Botd

## Provider metadata
- Category: `Security`
- Provider slug: `botd`
- Official pages manually reviewed:
  - `https://github.com/fingerprintjs/botd`
  - `https://fingerprint.com/bot-detection/`
- Manual review outcome: `manually_documented_no_public_http_api`
- Confirmed public HTTP routes in this pass: `0`

## Manual review result
The official pages reviewed in this pass describe Botd as a browser-side bot-detection library, not a public HTTP API.

## What the official pages currently show
- The GitHub repository title currently says: `Free bot detection library that runs in the browser. Detects automation tools and frameworks. No server required, runs 100% on the client.`
- The obvious current Fingerprint-hosted alternative page `https://fingerprint.com/bot-detection/` returned a Fingerprint `Not found` page instead of Botd API reference content.
- The autogenerated placeholder had surfaced `openfpcdn.io` asset URLs, but those are library/CDN distribution locations rather than documented Botd REST endpoints.

## What could be confirmed
- Botd is documented as a client-side JavaScript/browser library.
- The reviewed official material did not expose:
  - a public API base URL
  - REST endpoint paths
  - HTTP methods
  - request/response schemas
  - pagination rules
  - rate limits
  - public error schema
- Because the official material explicitly frames Botd as `No server required`, there is no grounded basis in this pass to treat it as a fireROUTE-routable HTTP provider.

## Confirmed routes
No public HTTP routes were manually confirmed.

Manual route count confirmed: **0**.

## fireROUTE note
Treat Botd as an SDK/library integration rather than a provider web API unless Fingerprint publishes current provider-controlled HTTP API documentation for it.
