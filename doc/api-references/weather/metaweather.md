# MetaWeather

## Provider metadata
- Category: `Weather`
- Provider slug: `metaweather`
- Official docs/site checked manually:
  - `https://www.metaweather.com/api/`
  - `https://www.metaweather.com/`
- Manual review outcome: `blocked - official domain unavailable`
- Manually confirmed routes in this pass: `0`

## Blocker summary
Both official MetaWeather URLs failed in live browser navigation with `net::ERR_NAME_NOT_RESOLVED`. The official domain therefore did not resolve at all during this verification pass, so no current official documentation or service surface was available to inspect.

## What could not be confirmed
- Whether the service is still live behind any official MetaWeather domain
- Current API base URL
- Current endpoint list
- Authentication model
- Response format details
- Error handling
- Pagination behavior
- Rate limits

## Important fireROUTE notes
- Treat this provider as officially unavailable until MetaWeather publishes a working official domain again.
- Do not rely on historical autogenerated route lists as authoritative without a fresh official source.
- This blocker is stronger than a transient docs-page issue: both the root and the `/api/` URL failed at DNS resolution during the same run.

## Verification notes
This file was manually rebuilt from live browser checks of the official MetaWeather root and API URL. Both failed with `ERR_NAME_NOT_RESOLVED`, so the provider remains a zero-route blocker.