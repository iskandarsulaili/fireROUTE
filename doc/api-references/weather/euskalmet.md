# Euskalmet

## Provider metadata
- Category: `Weather`
- Provider slug: `euskalmet`
- Official docs/site checked manually:
  - `https://opendata.euskadi.eus/api-euskalmet/-/api-de-euskalmet/`
  - `https://www.euskadi.eus/`
- Manual review outcome: `blocked - official docs URL no longer serves a live Euskalmet API reference`
- Manually confirmed routes in this pass: `0`

## What was confirmed live
- The official API-Euskalmet docs URL did not load a Euskadi-hosted API reference during live browser review.
- A fresh CDP navigation attempt to `https://opendata.euskadi.eus/api-euskalmet/-/api-de-euskalmet/` finished at `https://classify-web.herokuapp.com/#/api`.
- The final page title on that redirected destination was `No such app`.
- A separate live browser attempt to the official Euskadi government root `https://www.euskadi.eus/` timed out during navigation, so it did not provide a usable alternative route reference in this run.

## Blocker summary
The primary official docs URL for this provider now resolves to a dead Heroku-hosted application instead of a live Euskalmet/Open Data Euskadi API reference, and the official government root did not load successfully as an alternative during this browser-only pass. Because neither the original documentation URL nor the official alternative surface produced an inspectable route reference, I could not safely confirm route-level API details.

## What could not be confirmed from live official pages
- Canonical API base URL
- Endpoint inventory
- HTTP methods and parameter sets
- Authentication requirements
- Pagination behavior
- Error payload structure
- Response formats
- Rate-limit policy

## Important fireROUTE notes
- Keep this provider in explicit blocker status until Open Data Euskadi republishes a live Euskalmet route reference or a working official replacement page.
- Do not trust historical autogenerated route guesses as authoritative while the official docs URL resolves to a dead app.
- The blocker is now stronger than a generic timeout-only note: the original docs URL itself currently lands on a `No such app` page.

## Verification notes
This file was manually rebuilt from live browser checks of the original official docs URL and an official Euskadi government alternative page. The docs URL no longer exposes usable API documentation, so the provider remains a zero-route blocker.
