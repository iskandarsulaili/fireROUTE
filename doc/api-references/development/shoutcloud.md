# SHOUTCLOUD

## Provider metadata
- Category: `Development`
- Provider slug: `shoutcloud`
- Docs used manually:
  - `http://shoutcloud.io/`
  - `https://www.shoutcloud.io/`
- Confirmed live provider/API base URL: none in this session
- Authentication: unconfirmed
- Primary response format: unconfirmed
- Manually confirmed routes in this pass: `0`

## Blocker summary
The historical SHOUTCLOUD domain no longer presents API documentation in this session.

What I manually verified:
- `http://shoutcloud.io/` redirects to the current `https://shoutcloud.io/` site.
- `https://www.shoutcloud.io/` resolves to the same destination.
- The live site content is a Swedish gambling-information blog (`"Betting på utländska casino - Utan spelpaus"`), not the historical "ALL-CAPS AS A SERVICE" API.

## Authentication
- No current official API authentication method could be confirmed, because the reviewed official host no longer exposes API documentation or a functioning API landing page.

## Endpoint inventory
- No current official route-level API reference was reachable on the reviewed official host.
- No trustworthy base URL, path list, method list, or request schema could be confirmed from the repurposed site.

## Rate limits, pagination, and errors
- No current API rate-limit, pagination, or error-format documentation could be confirmed.

## Important usage notes
- The domain appears repurposed away from the original SHOUTCLOUD API.
- Because the current official host is unrelated to the advertised service, I did not preserve historical route assumptions from unofficial memory or third-party mirrors.
- FireROUTE should treat this provider as blocked/unverifiable unless a new official API home is identified.

## Verification notes
This file was manually rebuilt as an explicit blocker note after checking the indexed official host and its `www` variant. Both resolved to non-API gambling-blog content rather than SHOUTCLOUD documentation.